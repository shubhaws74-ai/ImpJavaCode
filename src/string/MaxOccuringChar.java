package string;
import java.util.*;
import java.util.stream.*;
public class MaxOccuringChar {
    //Find Maximum Occurring Character Input: "aabbbbcc" Output: 'b'
    public static void main(String [] args){
        String s="aabbbbcc";
//       Optional bv= s.chars().mapToObj(c->(char)c).collect(Collectors.groupingBy(c->c,Collectors.counting()))
//                .entrySet().stream().max(Comparator.comparing(e->e.getValue()));
//       System.out.println(bv.get());
        
    }
}
